# Week 2026-W17  (Apr 20 – Apr 26)

## Done
- Project bootstrapped: directory layout, CLAUDE.md, workflows, project skills.
- Initial Python scaffolding (FastAPI + 5-stage pipeline) — **migrated to TypeScript / Next.js 14** (App Router) on 2026-04-28 to unify with the planned React/Tailwind dashboard.
- Web app in place: Route Handlers for batches/leads/sites/pricing/webhooks, `lib/` modules for config/db/logger/retry/filters/pricing, services (Google Places, Outscraper, Claude, Cloudflare, Instantly, color extractor), and the 5 pipeline stages.
- Scraper switch wired: `batches.scraper` field, `lib/pricing.ts` cost estimator, `/api/pricing/estimate` + `/api/pricing/compare` endpoints. Default = `google_places` to use the $200/mo free credit.
- Database schema (`db/schema.sql`) + migration `002_add_scraper.sql`.
- Docs refreshed for the TS stack (architecture, stage_specs, deployment, data_model).

## In progress
- Awaiting first niche + city decision.
- Awaiting API keys and `.env` to be filled.

## Blocked
- Pricing model to set (setup + monthly vs. monthly only).
- Sending domain to use for cold email (separate from `optinetsolutions.com`).

## Numbers
| Metric | This week | Cumulative |
|--------|-----------|------------|
| Batches run | 0 | 0 |
| Leads scraped | 0 | 0 |
| Sites deployed | 0 | 0 |
| Emails sent | 0 | 0 |
| Replies | 0 | 0 |
| Meetings booked | 0 | 0 |
| Closes | 0 | 0 |
| MRR added | $0 | $0 |

## Next week
- Build the first Astro template (`templates/trades/`) to a real production-quality bar.
- Provision Supabase, apply `db/schema.sql`, smoke-test the API.
- Build the operator dashboard pages (frontend workstream — Next.js + Tailwind already wired).
- Buy + warm up a sending domain.
- Generate 3 hand-picked test sites end-to-end to validate the pipeline (Google Places + Claude live).

## Notes
- Reference: `Execution_Plan_v2.docx` is the source plan this build follows.
- ADR 0001 captures the template-vs-per-site decision.
