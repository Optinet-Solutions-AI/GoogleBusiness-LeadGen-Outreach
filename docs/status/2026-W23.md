# Status — 2026-W23 (Jun 1–7)

## Done
- **Voice agent test console** (`/test-call`): chat-style transcript, recording replay, editable
  prompt/intro/voice, in-app saved History (+ delete/clear-all). Voice tuned via the
  voice-agent-trainer skill (turbo TTS, gpt-4.1-nano, natural in-context fillers). **Now parked.**
- **Connected journey at $0** shipped: interested call → one-time SMS link → intake form → inbox
  (migration 021; Mobivate soft no-op until a key is set).
- **Apify scraper integrated + made the default** (migration 023; Places + Outscraper still
  selectable). Returns Maps data + website-crawled emails + FB/IG socials in one pass; ~$2/1k.
  Deployed to Vercel + the Cloud Run runner (image rebuilt, `APIFY_TOKEN` synced to Secret Manager).
- **Channel-by-segment outreach** wired:
  - has-website → **EMAIL**: free `website-email.ts` crawl (homepage + contact pages, ~50% hit) with a
    Playwright render fallback for JS sites; `stage-5-email.ts` + `email-sender.ts` (SMTP, soft no-op)
    + `POST /api/leads/[id]/email`.
  - no-website → **DM/SMS**: social page found + persisted (website_url/kind); `POST /api/leads/[id]/sms`
    fires the journey directly (not gated on a call).
  - Per-lead **channel badge** (Email vs DM/SMS) on the leads list.

## In progress
- Email/SMS sending run as **soft no-ops** until a mailbox (SMTP) + Mobivate key are connected — the
  full flow is provable at $0 today.
- Backfill of existing leads (re-run enrichment to populate emails/socials + measure real coverage).

## Blocked / waiting
- **Mobivate API key** → real SMS send.
- **A connected mailbox** (email_accounts SMTP creds) → real email send + warm-up.
- **Phone numbers** → live voice dialing (voice parked anyway).
- **DM tooling/decision** → automated FB/IG DM.

## Numbers
- Scrapers: Apify (default, +emails/socials), Outscraper, Google Places — all selectable.
- Email crawl hit-rate (sampled): ~50% on has-website leads, before the render fallback.

## Next
1. Backfill enrichment → real email/social coverage across the list.
2. Connect a mailbox → send a real test email (warm-up first).
3. Per-segment auto-routing (which channel fires per lead) + a "has email" filter.

### 2026-06-03

**task for today:**
- Made Apify the default lead scraper — pulls business info, emails, and Facebook/Instagram in one go (cheaper than Google, and the data's yours to keep + use).
- Kept Google Places and Outscraper as options you can still pick per batch.
- Turned on automatic email-finding: businesses that have a website get their contact email pulled in for you (~half of them, free).
- Set up the two ways to reach leads: has a website → email; no website → text/DM.
- Added a tag on every lead showing how you'll reach them (Email or DM/SMS).
- Re-checked all your existing leads for emails and filled them in; fixed a bug where it was grabbing fake "sample" emails.
- Got the production server updated so Apify scraping works there too, not just your laptop.
- Result: scraping → finding emails → sorting leads by how to contact them is now automatic; new batches just work.
- Still to do next time: connect a real mailbox + SMS number so it actually sends (it's built — just in "test mode" for now).

**pending/still in progress:**
- Connect an email mailbox so emails really send (currently safe test-mode, no real emails go out).
- Add the SMS key (Mobivate) so texts really send.
- Auto-routing so the right channel fires per lead on its own.
- Facebook/Instagram DM sending — needs a tool/decision.
- Live phone calls (voice agent) — parked, waiting on phone numbers.

### 2026-06-04

**task for today:**
- Redesigned the whole app — cleaner modern look, faster page switches, and pop-up confirmations so you know an action worked.
- Made the Leads page a pick-and-choose workspace: tick the exact leads you want and add them straight to a campaign.
- Rebuilt the "New campaign" wizard — it shows the real list of leads (not just a number), lets you pick exactly which ones, sets the country's time zone automatically, lets you choose which inbox sends, and lets you send yourself a test before launching.
- You can now connect your own email (Gmail, Outlook, Titan, or any mailbox) as a sender.
- Result: the full path — pick leads → build a campaign → choose your sender → test on yourself → launch — now works in one place and is fully live.
- Still to do next time: send a first real test email to yourself, then start a small real send (warm up slowly).

**pending/still in progress:**
- SMS key (Mobivate) so texts really send.
- Facebook/Instagram DM sending — needs a tool/decision.
- Live phone calls (voice agent) — parked, waiting on phone numbers.

### 2026-06-05

**task for today:**
- Proved your email actually sends — fired a real test from your connected inbox and it landed; added a one-click "Send test" on every mailbox so you can re-check any of them anytime.
- Turned the Inbox into a real two-way tool — replies show up there and you can reply right from the app; it also shows a lead's submitted form answers and how to reach them when there's no email.
- Added an "Assisted DM" helper + a dedicated "Social" section for Instagram/Facebook-only leads — a ready-to-send message + open-their-profile + mark-sent, with one shared account the whole team works from (Instagram bans automated cold DMs, so this keeps the account safe).
- Added an email filter on Leads (has email / no email / verified) so you can target campaigns at reachable people.
- Fixed a hidden bug that was hiding ALL your Inbox conversations on the live site.
- Made lead-gathering faster — the app can now run several scraping jobs at once instead of one at a time (live on the cloud).
- Made the whole app noticeably snappier — pages load quicker by fetching data smarter and remembering recent results.
- Built and launched email verification — before anything goes out, each address is checked as real / risky / dead (via ZeroBounce), so you stop emailing dead addresses and protect your sender reputation. Tested live with your key (it correctly tagged real vs. catch-all vs. unknown addresses).
- Result: your outreach is now provably working (sends + replies), faster, cleaner, and protected — and every email is verified before it goes out so your domain stays healthy.
- Still to do next time: verify your existing lead list in bulk, then start a real send.

**pending/still in progress:**
- Bulk-verify the whole lead list: needs the cloud verify-job switched on (one quick rebuild). Note ZeroBounce free tier is 100 checks/month — upgrade if your list is bigger.
- Heads-up: verification is now ON, so the app will hold any unchecked lead from being emailed until it's verified — run a verify pass before sending.
- The "one inbox for everything" monitoring hub (email + DM + SMS + calls in one place, with conversion rates per campaign) is fully designed/written up but not built yet — parked, ready when you want it.
- SMS sending (Mobivate key) and live phone calls (voice agent) — still off/parked.
