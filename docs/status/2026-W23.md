# Status — 2026-W23 (Jun 1–7)

## Done
- **Voice agent test console** (`/test-call`): chat-style transcript, recording replay, editable
  prompt/intro/voice, in-app saved History (+ delete/clear-all). Voice tuned via the
  voice-agent-trainer skill (turbo TTS, gpt-4.1-nano, natural in-context fillers). **Now parked.**
- **Connected journey at $0** shipped: interested call → one-time SMS link → intake form → inbox
  (migration 021; Mobivate soft no-op until a key is set).
- **Apify scraper integrated + made the default** (migration 023; Places + Outscraper still
  selectable). Returns Maps data + website-crawled emails + FB/IG socials in one pass; ~$2/1k.
  Deployed to Vercel + the Cloud Run runner (image rebuilt, `APIFY_TOKEN` synced to Secret Manager).
- **Channel-by-segment outreach** wired:
  - has-website → **EMAIL**: free `website-email.ts` crawl (homepage + contact pages, ~50% hit) with a
    Playwright render fallback for JS sites; `stage-5-email.ts` + `email-sender.ts` (SMTP, soft no-op)
    + `POST /api/leads/[id]/email`.
  - no-website → **DM/SMS**: social page found + persisted (website_url/kind); `POST /api/leads/[id]/sms`
    fires the journey directly (not gated on a call).
  - Per-lead **channel badge** (Email vs DM/SMS) on the leads list.

## In progress
- Email/SMS sending run as **soft no-ops** until a mailbox (SMTP) + Mobivate key are connected — the
  full flow is provable at $0 today.
- Backfill of existing leads (re-run enrichment to populate emails/socials + measure real coverage).

## Blocked / waiting
- **Mobivate API key** → real SMS send.
- **A connected mailbox** (email_accounts SMTP creds) → real email send + warm-up.
- **Phone numbers** → live voice dialing (voice parked anyway).
- **DM tooling/decision** → automated FB/IG DM.

## Numbers
- Scrapers: Apify (default, +emails/socials), Outscraper, Google Places — all selectable.
- Email crawl hit-rate (sampled): ~50% on has-website leads, before the render fallback.

## Next
1. Backfill enrichment → real email/social coverage across the list.
2. Connect a mailbox → send a real test email (warm-up first).
3. Per-segment auto-routing (which channel fires per lead) + a "has email" filter.

### 2026-06-03 — end of task
- **Done:** Apify is the default scraper (Maps + emails + socials; live on web + Cloud Run). Email channel for has-website leads + DM/SMS lane for no-website (both $0 soft no-op). Email backfill ran → 40% coverage; placeholder-email bug fixed. Per-lead channel badge added.
- **Your action items:** connect a mailbox (SMTP) for real email; add `MOBIVATE_API_KEY` for real SMS; warm the email domain ~2 weeks before volume.
- **Blocked:** phone numbers (live voice), DM tooling (FB/IG automation).
- **Next:** per-segment auto-routing; "has email" filter; first real send once a mailbox/key is connected.
