# Status — 2026-W25 (Jun 15–21)

## Job for today (2026-06-18)

- **Pulled your whole website-design library into the project.** Downloaded all 15 designs from the Google Drive folder — 3 per niche across Auto, Chiropractic, Dental, Restaurants, and Trades — and organized them.
- **You can now choose which design each prospect's demo uses.** When you start a batch you pick a default design for that niche, and you can override it per business right on the Build button. Pick from 3 looks per niche instead of one fixed template. Live in the dashboard now.
- **Every design auto-fills the prospect's real details** — business name, phone, address, and brand color drop in automatically. For the 10 standard designs, their real Google reviews and opening hours flow in too. A "Preview" link lets you eyeball any design before choosing.
- **The 3 fancy "app-style" designs (one per niche) currently fill in name/contact/color only.** They're built with a complex page-builder export we can't fully safety-check without opening them in a real browser, so for now they keep their own sample reviews/hours. Pushing their real reviews/hours in is a follow-up once we can eyeball them.
- Result: the demo sites are no longer one-look-per-niche — you have 3 polished options per niche, picked per prospect, all auto-personalized and live in production.
- Still to do next time: open the 3 fancy designs in a browser to confirm they look right, then switch on real reviews/hours for them; otherwise nothing blocking.

## Job for today (2026-06-16)

- **Shipped the screenshot-first cold-email sequence (live).** The headline outreach feature: a 4-step trust ladder (plain text → screenshot → live link → soft close, 4 days apart) that runs on the cloud and auto-sends ~every 15 min once a lead is enrolled — and never follows up a bounced/blocked address. Retired the old Instantly path.
- **Built 5 single-file niche site templates + a build gate** — faster, cleaner site generation for the focus niches (trades, dental, chiro, restaurant, auto).
- **Published in-app docs** — an API reference (/api-docs) and a user manual (/manual) with the workflow laid out step-by-step.
- **Rebranded the app to RateUp** (from Optirate).
- **Email accounts + leads polish** — remove-mailbox button, lead name links to its real Google listing, Google web-search fallback.
- **Connected the project to the external PMS** + built a tool that reads/updates it automatically (and filled in its previously-blank description). The "API key" the PMS advertises is dead on the live site, so we use the browser login session.
- Result: the core money-making loop (scrape → build → screenshot → email sequence) is now live end-to-end, the app is rebranded, documented, and self-reporting into the PMS.
- Still to do next time: enroll the first leads + start the email warm-up; optionally schedule the PMS sync; refresh the PMS login token when it expires (~30 days).

## Done — full project timeline

- **2026-04-28 — Project kicked off.** Built the foundation of the whole system in one modern codebase — the app, the operator dashboard, and the first ready-to-use business-website template (with search-engine basics built in).
- **2026-04-29 to 04-30 — Lead control + reach.** Taught the system to filter out bad leads and let you hand-pick which businesses to build for; expanded reach from 13 to 44 countries (70 → 210 cities); moved the heavy work onto Google's cloud so big jobs run reliably.
- **2026-05-01 to 05-04 — Sites got genuinely good.** AI now writes tailored copy for each business and picks colors, fonts and layout to fit the trade, embeds a real Google map, and the sites hit top speed/SEO scores. Added a one-click "Rebuild" for any site.
- **2026-05-21 — First mailbox + richer templates.** Connected a real sending mailbox (Titan/Bluehost); grew the template library, with colors pulled from each business's own photos.
- **2026-05-25 to 05-26 — Photos & logos automated.** Split businesses into 20 industry types, each with its own photo set; the AI picks the best photos and pulls real logos from a business's Facebook/Instagram automatically.
- **2026-05-27 to 05-28 — Accuracy + variety + QC.** Smarter location matching (no more mixing up same-named towns); made every generated site look different from the next; added an automated quality-checker for finished demo sites.
- **2026-05-29 — Strategy shift to voice-first.** The system now sizes up each business and routes it to the right offer (build a new site / improve their old one / an AI phone agent). Sites also got far more visual variety per industry.
- **2026-06-01 to 06-02 — Campaign system + test calls.** Built campaigns: group leads into 3 types, import from the app / a spreadsheet / by hand, run campaign queues, and see a full funnel dashboard. Added an in-app AI voice agent you could test-call from the browser.
- **2026-06-03 — Cheaper scraping, all-in-one data.** Switched the lead scraper to Apify — cheaper, and it pulls business info, emails, and Facebook/Instagram in a single pass (and the data is yours to keep). Leads now get sorted by how to reach them (email vs text/DM), and the full "interested call → text a link → fill a form → land in your inbox" journey worked at zero cost.
- **2026-06-04 — Redesign + campaign wizard.** Full app redesign (cleaner, faster); Leads became a tick-and-add workspace; rebuilt the campaign wizard (shows the real list, sets the country's time zone automatically, pick your sender, test on yourself); connect any mailbox (Gmail/Outlook/Titan/custom).
- **2026-06-05 — Real sending proven + verification launched.** Proved a real email sends and lands; made the Inbox two-way (reply from inside the app); added a safe "Assisted DM" helper for Instagram/Facebook-only leads; launched email verification so every address is checked real / risky / dead before sending — protecting your sender reputation.
- **2026-06-08 — Second checker + clean list.** Added a second email checker (Hunter) for higher accuracy; verified your whole list (good-to-send emails went from 28 to 63; 74 sendable in total); made the two-checker verification run automatically in the cloud on every future scrape.
- **2026-06-09 to 06-10 — Reporting + exports.** Refreshed the CRM look; added an analytics dashboard (7 key numbers with week/month/year and custom date ranges); per-batch CSV export of phone-reachable leads; accurate dating of meetings and deals for clean reporting.
- **2026-06-15 — Streamlined + voice spun off.** Removed the in-app calling/voice feature (it's moving to its own separate app); added an automatic website-health check plus a manual lead-type override; rebuilt the dashboard's filtering and search and made it self-generate a weekly status page.
- **2026-06-16 — PMS connected.** Connected the project to your external PMS and built a tool that updates it automatically; filled in the project's description (see *Job for today* above).

## In progress / pending

- **First real email campaign not sent yet** — the list is clean (74 sendable); it needs a slow warm-up start before any volume goes out.
- **170 of 252 leads still have no email** — finding those grows your sendable list more than anything else.
- **SMS sending not live** — it's the biggest channel (~125 phone-reachable leads) and it's built, but waiting on the text-sending key (Mobivate).
- **Facebook/Instagram DM sending is manual ("assisted") on purpose** — fully automating it risks the account being banned.
- **PMS auto-sync runs when asked** — a scheduled, fully-hands-off version isn't set up yet, and the browser login token it uses expires about every 30 days.
- **Voice / phone-calling is parked here** — it's moving to a separate app.
